[workspace.package]
license = "MIT"
version = "0.1.0"

[workspace.dependencies]
ego-tree = "0.6.2"

[workspace]
members = ["tui", "core"]
resolver = "2"

[profile.release]
opt-level = "z"
debug = false
lto = true
codegen-units = 1
panic = "abort"
strip = true
incremental = false
