name: nix

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  checks:
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule'

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: set up nix
        uses: cachix/install-nix-action@4e002c8

      - name: check formatting
        run: |
          nix fmt
          if [ -n "$(git status --porcelain)" ]; then
            git --no-pager diff
            exit 1
          fi

      - name: check flake evaluation
        run: nix flake check

  update:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: set up nix
        uses: cachix/install-nix-action@4e002c8

      - name: update flake inputs
        run: nix flake update

      - name: open pull request
        uses: peter-evans/create-pull-request@5e3a766de399b753aa7c86dd6ee63693e1fe0ac1 # v8
        with:
          commit-message: 'chore(nix): update flake inputs'
          committer: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          branch: 'chore/nix/flake-update'
          title: 'chore(nix): update flake inputs'
          reviewers: adamperkowski
          assignees: adamperkowski
